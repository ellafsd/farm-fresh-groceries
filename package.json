{
  "name": "farm-fresh-groceries",
  "version": "1.0.0",
  "description": "Small Node.js app that serves a grocery overview and per-product pages using plain HTML templates and JSON data.",
  "main": "index.js",
  "private": true,
  "scripts": {
    "start": "node index.js",
    "dev": "nodemon --watch index.js --watch modules --watch templates --watch dev-data --ext js,json,html index.js"
  },
  "keywords": ["node", "http", "templating", "groceries"],

  "devDependencies": {
    "nodemon": "^3.0.0"
  }
}